# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a new Python project (configured for conda in .vscode/settings.json).

## Commands

- Activate environment: `micromamba activate novel`
- Install dependencies: `micromamba install` or `pip install -e .`
- Run tests: `pytest` or `python -m pytest`
- Lint: `ruff check .` or `flake8`

## Architecture

None yet - project structure to be determined as code is added.
